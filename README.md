# Biblical Reference
> That is a development repo. If you want to download this add-on do it directly from Anki, code: `1639590852`.

# To start developing:
1. Create folder in `anki21`.
2. Copy this repo.
3. Fill the vendor.

```Bash
pip install -r vendor_BiblicalReference_requirements.txt --target ./vendor_BiblicalReference
```
